Run `npm run build` or `npm run watch` to either build the source or build it continuously in a watch mode.

Navigate browser to http://localhost:8080

Click on the browser window.

Allow mic access.

Enjoy!
